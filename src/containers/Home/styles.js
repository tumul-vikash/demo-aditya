import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    padding: 5,
  },
  CartButton: {
    backgroundColor: 'orange',
    alignItems: 'center',
    padding: 10,
    margin: 10,
  },
  CartText: {
    color: '#fff',
    fontSize: 20,
    fontWeight: '600',
  },
});
